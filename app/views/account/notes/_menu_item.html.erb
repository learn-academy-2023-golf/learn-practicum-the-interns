<% if can? :read, Note.new(team: current_team) %>
  <%= render 'account/shared/menu/item', {
    url: main_app.polymorphic_path([:account, current_team, :notes]),
    label: t('notes.navigation.label'),
  } do |p| %>
    <% p.icon do %>
      <i class="<%= t('notes.navigation.icon') %>"></i>
    <% end %>
  <% end %>
<% end %>
